// scripts/apply-blueprint.mjs
import { execa } from 'execa';
import fs from 'fs/promises';

async function generateReport(issueNumber, operations, error = null) {
  let report = `## Blueprint Agent Report: #${issueNumber}\n\n`;
  if (error) {
    report += `**STATUS: FAILED** 🔴\n\n`;
    report += `**ERROR:**\n\`\`\`\n${error.stack || error.message}\n\`\`\`\n`;
  } else {
    report += `**STATUS: SUCCESS** 🟢\n\n`;
    report += `**Applied Changes:**\n`;
    if (operations.length > 0) {
      operations.forEach(op => {
        report += `- [x] Wrote ${op.content.length} bytes to \`${op.path}\`\n`;
      });
    } else {
      report += `- No file operations were specified.\n`;
    }
  }
  report += `\n---\n*This report was auto-generated by the Blueprint Agent.*`;
  return report;
}

async function main() {
  console.log('🔵 Blueprint Agent: Activated. Searching for new blueprints...');

  let issueNumber;
  const operations = [];

  try {
    // 1. Fetch the latest open issue
    const { stdout: issueJson } = await execa('gh', [
      'issue', 'list', '--state', 'open', '--limit', '1', '--json', 'number,body'
    ]);

    if (!issueJson || issueJson.trim() === '[]') {
      console.log('🟢 No new blueprints found. Standing by.');
      return;
    }

    const [issue] = JSON.parse(issueJson);
    issueNumber = issue.number;
    const { body } = issue;

    console.log(`🔵 Found Blueprint #${issueNumber}. Preparing to build...`);

    // 2. Parse the blueprint body
    const fileRegex = /--- START OF FILE (.*?) ---\n([\s\S]*?)\n--- END OF FILE ---/g;
    let match;

    while ((match = fileRegex.exec(body)) !== null) {
      const filePath = match[1].trim();
      const fileContent = match[2];
      operations.push({ path: filePath, content: fileContent });
    }

    if (operations.length === 0) {
      console.log('🟡 Blueprint is empty or malformed. Closing issue and standing by.');
      const report = await generateReport(issueNumber, operations);
      await execa('gh', ['issue', 'comment', issueNumber.toString(), '--body', report]);
      await execa('gh', ['issue', 'close', issueNumber.toString()]);
      return;
    }

    // 3. Apply changes
    for (const op of operations) {
      console.log(`- Writing to ${op.path}...`);
      await fs.writeFile(op.path, op.content, 'utf-8');
    }

    console.log('✅ Build complete according to blueprint.');

    // 4. Commit, Push, and Report
    console.log('🔵 Committing changes and generating report...');
    await execa('git', ['add', '.']);
    await execa('git', ['commit', '-m', `feat: Apply blueprint from Issue #${issueNumber}`]);
    await execa('git', ['push']);

    const report = await generateReport(issueNumber, operations);
    await execa('gh', ['issue', 'comment', issueNumber.toString(), '--body', report]);
    await execa('gh', ['issue', 'close', issueNumber.toString()]);

    console.log(`🟢 Blueprint #${issueNumber} applied and closed. Report posted.`);

  } catch (error) {
    console.error('🔴 An error occurred in the Blueprint Agent:', error);
    if (issueNumber) {
      const report = await generateReport(issueNumber, null, error);
      await execa('gh', ['issue', 'comment', issueNumber.toString(), '--body', report]);
    }
    process.exit(1);
  }
}

main();